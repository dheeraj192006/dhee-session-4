#include <stdio.h>
int largest (inta,intb,intc){
  if(a>=b&&a>=c) return a;
  if(b>=a&&b>=c) return b;
  return c;
}
int main(){
  int x,y,z;
  printf("Enter three numbers:");
  scanf("%d%d%d",&x,&y,&z));
  return 0;
}
